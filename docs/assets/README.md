# Report figures

PNG files here are used by the main README (dark theme, English labels).

- **Committed by default:** real training/eval figures (`figure_training_curves.png`, `figure_eval_summary.png`).
